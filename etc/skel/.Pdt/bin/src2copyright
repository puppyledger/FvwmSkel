#! /usr/bin/perl

my $VERSION = '2019-09-13.04-04-50.EDT';

use Cwd;
use strict;

my $PROJECTPATH = $ENV{'PDT_ROOT'} . '/' . $ENV{'$PDT_ACTIVE'};
print "processing $PROJECTPATH for copyright submission\n";

chdir $PROJECTPATH;

print $PROJECTPATH ;

#open( FIND, "find | grep -ve \.EDT | grep -ve \.git | grep -ve \.config | grep -v Examiner | grep -v Handling |" );
#
#while (<FIND>) {
#   print $_ ;
#}

